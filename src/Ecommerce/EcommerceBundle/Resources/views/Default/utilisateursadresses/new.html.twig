{% extends 'layout/layout.html.twig' %}

{% block body %}
    <h1>Ajouter Adresses</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('utilisateursadresses_index') }}">Retour a la liste</a>
        </li>
    </ul>
{% endblock %}
