{% extends 'layout/layout.html.twig' %}

{% block body %}
    <h1>Ajouter Adresses</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    <a href="{{ path('utilisateursadresses_index') }}" class="button alt pull-right">Retour a la liste</a>
    {{ form_end(form) }}




{% endblock %}
